import React from 'react';
import { Box, Typography } from '@mui/material';

const HomePage = () => {
  return (
    <Box sx={{ textAlign: 'center', marginTop: '50px' }}>
      <Typography variant="h3">Bienvenido a la página principal</Typography>
    </Box>
  );
};

export default HomePage;
